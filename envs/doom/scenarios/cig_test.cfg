# Lines starting with # are treated as comments (or with whitespaces+#).
# It doesn't matter if you use capital letters or not.
# It doesn't matter if you use underscore or camel notation for keys, e.g. episode_timeout is the same as episodeTimeout.

doom_scenario_path = cig_test.wad

#12 minutes
episode_timeout = 25200


# test rewards
living_reward = 0.00001
death_penalty = 0.1

# Rendering options
screen_resolution = RES_640X480
screen_format = CRCGCB
render_hud = true
render_crosshair = true
render_weapon = true
render_decals = false
render_particles = false

window_visible = true

# Available buttons

available_buttons = {
        MOVE_FORWARD
        MOVE_BACKWARD
        MOVE_RIGHT
        MOVE_LEFT
        TURN_RIGHT
        TURN_LEFT
        ATTACK
        SPEED
}

# Game variables that will be in the state
available_game_variables = { POSITION_X POSITION_Y ANGLE SELECTED_WEAPON SELECTED_WEAPON_AMMO HEALTH USER2 }

mode = PLAYER
